# Set to postgres for Vercel Postgres mode, or client for localStorage fallback
NEXT_PUBLIC_STORAGE_MODE=postgres

# Required when NEXT_PUBLIC_STORAGE_MODE=postgres
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"
